version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: controle-reembolso-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: controle-reembolso-db
    ports:
      - '5432:5432'
    
